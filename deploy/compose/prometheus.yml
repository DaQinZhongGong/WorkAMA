global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/workama.yml

scrape_configs:
  - job_name: workama-otel
    static_configs:
      - targets: [otel-collector:9464]
        labels:
          project: workama
